
data_config = dict(
    evaluate= False,
    db_root = "path/train",
    test_root = "path/test",
    validation_root="path/val",
    normalize = dict(l1 = False, l2= False)
)

config = dict(data_config = data_config)